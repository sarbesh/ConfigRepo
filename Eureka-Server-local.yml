---
logging:
  level:
    ROOT: DEBUG
    com.sarbesh.eurekaserver: DEBUG

eureka:
  instance:
    preferIpAddress: true
  client:
  client:
	registerWithEureka: false
 	fetchRegistry: false
 	serviceUrl:
    	defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

